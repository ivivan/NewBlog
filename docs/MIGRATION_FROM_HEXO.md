# Hexo -> Astro migration checklist

This project is the migration target for `/Users/yifanzhang/Code/Blog`.

## Progress snapshot (2026-04-02)

- Astro project scaffolded and building.
- Hexo content imported:
	- `source/_posts/*` -> `src/content/blog/*`
	- `source/images/*` -> `public/images/*`
	- `source/CNAME` -> `public/CNAME`
- Site URL set to `https://ivivan.com`.
- Schema made tolerant for legacy frontmatter differences (missing/empty tags, optional fields).
- Legacy permalink route added and generating:
	- `/YYYY/MM/DD/slug/`
- Duplicate post route removed:
  - removed `/blog/:slug/` page route to avoid duplicate content.
- Build status: success (`671` static pages generated after route cleanup).

## Immediate next actions

1. Canonical post route behavior:
- Done: using only `/YYYY/MM/DD/slug/` as canonical post URLs.

2. Replace remaining starter pages with site-specific pages:
- links, about, 404.

3. Theme parity pass:
- post detail page styles, archives/taxonomy styling, typography polish.

- Hexo posts: `source/_posts/*.md`
- Astro posts target: `src/content/blog/*.md`

- Hexo pages:
- `source/about/index.md` -> `src/pages/about.astro` (or `src/pages/about/index.astro`)
- `source/link/index.md` -> `src/pages/link.astro`
- `source/category/index.md` -> `src/pages/categories.astro` (index of categories)
- `source/tag/index.md` -> `src/pages/tags.astro` (index of tags)
- `source/404.html` -> `src/pages/404.astro`

- Hexo static files:
- `source/images/**` -> `public/images/**`
- `source/CNAME` -> `public/CNAME`
- Do not copy old `source/sitemap.xml` (Astro generates this via integration).

## 3) Frontmatter mapping

Current schema supports both styles during migration.

- `title` -> `title`
- `date` (Hexo) -> `pubDate` (Astro internal)
- `updated` (Hexo) -> `updatedDate`
- `tags` -> `tags`
- `categories` -> `categories`
- `description` -> `description` (optional)
- `slug` -> `slug` (optional)

## 4) Route compatibility targets

To preserve SEO and backlinks, keep post URLs in the old pattern:

- Current Hexo pattern: `/YYYY/MM/DD/slug/`
- Astro target should match the same permalink output.

If any URL must change, add explicit redirects before cutover.

## 5) First content migration pass

From old repo root (`/Users/yifanzhang/Code/Blog`):

```bash
mkdir -p ../Blog-astro/src/content/blog
cp -R source/_posts/* ../Blog-astro/src/content/blog/
cp source/CNAME ../Blog-astro/public/CNAME
cp -R source/images ../Blog-astro/public/
```

Then in Astro repo:

```bash
npm run dev
npm run build
```

Fix frontmatter errors iteratively (especially invalid dates and malformed YAML).

## 6) Theme parity order (recommended)

1. Typography and spacing baseline
2. Header/footer/nav
3. Post list and post detail templates
4. Archive page
5. Tag/category pages
6. Code block style and TOC behavior

## 7) SEO checks before cutover

- Canonical URL domain is `https://ivivan.com`
- `public/CNAME` exists
- Sitemap generated by Astro
- RSS feed is valid
- Open Graph/Twitter metadata present for posts
- 404 page works

## 8) Deployment cutover strategy

- Deploy Astro to preview first (GitHub Pages preview branch or Cloud provider preview).
- Compare top historical URLs from analytics/search console.
- When parity is acceptable, point production deployment to Astro.
- Keep Hexo repo intact for rollback for at least 1-2 weeks.
